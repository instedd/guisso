= content_for :instedd_content do
  %center
    = yield :form_title

  .row
    .span3.centred
      .box
        = yield

      - if content_for?(:after_form)
        = yield :after_form

= render template: 'layouts/application'
